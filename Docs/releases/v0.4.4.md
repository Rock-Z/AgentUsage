# AgentUsage 0.4.4

Claude authorization now refreshes automatically when its access token expires.

- Delegates refresh-token handling to Claude Code, avoiding conflicting token rotation.
- Retries Claude usage once after refresh and prevents repeated refresh attempts.
- Keeps the preserved credential helper unchanged across app updates.
